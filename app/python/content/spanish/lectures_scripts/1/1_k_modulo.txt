Bueno, permítame proponer que pivoteemos ahora hacia otro enfoque aquí que use otro símbolo que, hasta ahora, no hemos tenido ocasión de usar realmente. Permítame proponer que implementemos un programa que llamaremos paridad. En matemáticas, paridad puede referirse a si un número es par o impar. Y esa es una pregunta interesante. Y resulta ser útil en otras aplicaciones también, solo para preguntar si un número dado es par o impar, tal vez el que el usuario haya escrito.

Y déjeme continuar y escribir un nuevo programa llamado paridad.py, mediante el código paridad.py en mi terminal. Y permítame proponer que usemos esta oportunidad para presentar el último de esos símbolos aritméticos, al menos la mayoría de los cuales nos resultan familiares: suma, resta, multiplicación, división. Pero hay uno que no ha estado en esta lista antes, este último aquí, el signo de porcentaje. Y no significa porcentaje en este caso, cuando se usa como operador en programación en Python.

En cambio, representa el llamado operador módulo, para la aritmética modular. O, al menos en nuestro caso, lo usaremos para calcular el resto al dividir un número por otro. ¿Qué quiero decir con eso? Bueno, si tomas un número como uno dividido por tres, tres no encaja exactamente en uno. Entonces tienes un resto de uno. Dos dividido por tres tiene un resto de dos. Tres dividido por tres tiene un resto de cero, porque se divide correctamente.

Cuatro dividido por tres tiene un resto de uno, porque puedes dividirlo una vez, pero luego queda uno, por lo que tiene un resto de uno. Y, por último, algo como cinco dividido por tres tiene, por supuesto, un resto de dos. Eso es lo que queremos decir con resto, cuánto queda después de dividir un número por otro. Bueno, si vuelvo ahora a mi código y considero cómo podría implementar la pregunta de si este número es par o impar, consideremos cómo podríamos hacerlo, porque quizás no sea obvio cómo se puede utilizar este bloque de construcción adicional.

Pero resulta que será muy útil a largo plazo. Bueno, primero obtengamos un número del usuario en una variable llamada x. Y voy a establecer ese igual a la conversión a int de lo que ingrese el usuario, después de preguntarles qué es x, ¿verdad? Y hemos hecho eso antes, muchas veces. ¿Cómo determino ahora si x es par o impar? Bueno, resulta que si tengo acceso a un operador de programa que me dice el resto, creo que puedo hacer esto.

De hecho, déjame preguntar al grupo. Y esto es solo de las matemáticas de la escuela primaria, quizás, ¿qué significa que un número sea par? Para ser claro, números como 0, 2, 4, 6, 8, 10, 12, 14, 16, todos esos son números pares. Pero, ¿qué significa realmente eso? Elena, si lo digo bien?

ELENA: Números pares que se pueden dividir exactamente por dos. Por ejemplo, 2, 4, 6, 8 y 10, y--

DAVID MALAN: Perfecto. Y podríamos seguir todo el día, literalmente, ya que hay un número infinito de esos números pares. Pero es bueno que lo hayas formulado en términos de una pregunta que podemos hacer de manera muy clara. ¿Se puede dividir este número de forma limpia por dos? Es decir, ¿podemos dividirlo por dos sin resto, con un resto de cero? Bueno, eso es perfecto, porque si tenemos este operador, este signo de porcentaje, que nos permite responder justo eso, cuál es el resto, presumiblemente podemos verificar si el resto es cero o uno. ¿No nos queda nada, o nos queda uno?

Bueno, preguntémoslo. Si x dividido por dos tiene un resto de cero, como lo propone Elena, adelante e imprimamos algo como "par", y simplemente le digamos eso al usuario. Sino, creo que podemos asumir que si un número no es par, será impar, si de hecho es un número entero. Así que voy a continuar e imprimir "impar" en su lugar. Y vamos a ejecutar ahora Python de paridad.py en mi consola. ¿Qué es x? Comencemos con dos. Dos es, de hecho, par.

Comencemos con cuatro. Cuatro es, de hecho, par. Vamos a ser interesantes con tres. Tres es ahora impar. Y podríamos hacer eso todo el día y espero obtener exactamente esa respuesta. ¿Pero qué más podríamos hacer aquí? ¿Cómo podríamos mejorar esto? Bueno, recordemos que tenemos la capacidad de inventar nuestras propias funciones. Y permítame proponer, para fines de discusión, que eventualmente encontraremos que es útil poder determinar si un número es par o impar.

Y nos gustaría tener esa funcionalidad incorporada. Y no creo que Python tenga una función que me indique justo eso. Pero puedo inventarla usando un código como este. Entonces, permítame ir a mi versión anterior aquí. Y permítame proponer que hagamos esto. Permítame continuar y escribir una función principal. Voy a volver a ese hábito de definir una función principal para representar la parte principal de mi programa. Y voy a hacer lo que hice antes. Voy a obtener un número entero de la entrada del usuario, preguntándoles qué es x, ¿verdad?

Y luego voy a hacer esta pregunta. Por el momento, voy a asumir ingenuamente que la función ya existe, pero eso es una técnica útil para resolver problemas. Incluso si no tengo idea de a dónde voy con esto, cómo voy a inventar una función que determine si un número es par, simplemente voy a asumir que hay una función llamada "es par", y la voy a llamar, a ciegas, así. Si es par, pasándole x, entonces continuamos e imprimimos la palabra "par".

Entonces, si esta función mágica llamada "es par" devuelve verdadero, como su valor de retorno, voy a imprimir que es par. Sino, de lo contrario, asumimos que es, por supuesto, impar. Ahora, el único problema con este programa, incluso si llamo a main aquí, es que "es par" no existe. Y este programa se rompería si lo ejecutara en este momento. Pero eso está bien. Tengo la capacidad, recordemos, de inventar mi propia función. Entonces déjeme definir, con def, una función llamada "es par".

Quiero que esta función tome un argumento. Y lo voy a llamar n, solo un número, genéricamente. Podría llamarlo x. Pero de nuevo, no quiero confundirme sobre cuál x es cuál. Entonces, le voy a dar un nombre diferente, y está bien. Solo voy a llamarlo, más genéricamente, n para número. Y luego voy a hacer esto. Voy a decir si N % dos es igual a cero, igual que antes, entonces, y aquí está la magia, tú, el programador, realmente puedes devolver lo que se llaman valores booleanos.