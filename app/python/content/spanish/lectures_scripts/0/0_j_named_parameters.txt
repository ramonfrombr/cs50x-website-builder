Permíteme volver al código en cuestión y ver si puedo mostrarte un par de formas diferentes de resolver el mismo problema, enfatizando que técnicamente estamos hablando de cadenas de texto, pero en Python hay incluso un término técnico para estas cadenas, que es STR, por así decirlo, S-T-R abreviado para string. Como tal vez sepas si has programado en otros lenguajes, a las personas que inventan lenguajes de programación les gusta ser muy concisos y directos, así que tendemos a usar frases bastante cortas para describir las cosas, no necesariamente palabras completas. Entonces, aunque podrías decir "string", técnicamente en Python lo que estamos realmente hablando, estas secuencias de texto, son técnicamente STRs. Este es un tipo de dato real en un programa. Pero pronto veremos que también hay otros tipos de datos en los programas. De hecho, veamos si podemos mejorar esto de otra manera.

Me gusta el progreso que hemos logrado al mantener todo en la misma línea, "Hello, David" todo en la misma línea. ¿Qué más podríamos hacer para resolver este problema? Bueno, resulta que no tuvimos que renunciar por completo a usar "print" dos veces. Déjame retroceder un poco y volver a esa versión anterior donde no estaba realmente seguro de cómo resolver este problema, así que usé "print" una vez para imprimir solo "Hello" y el espacio y la coma. Y luego usé "print" nuevamente para imprimir el nombre. Eso, estrictamente hablando, no estuvo mal, pero había este efecto visual que no me gustó. Simplemente se veía feo tener estas dos líneas de texto separadas una de la otra. Pero hay otra forma de solucionar esto.

Claramente parece ser el caso de que la función "print" está generando automáticamente una línea en blanco. Está moviendo el cursor automáticamente a la siguiente línea porque es por eso que veo "Hello" en una línea y "David" en la siguiente, y luego mi indicador, el símbolo de dólar, en la línea siguiente a esa. Entonces, "print" parece presumir automáticamente que quieres que mueva el cursor a la siguiente línea después de pasarle un argumento. Pero puedes anular ese comportamiento. Nuevamente, las funciones toman argumentos que influyen en su comportamiento, solo tienes que saber cuáles son esos argumentos. Y resulta que si revisamos la documentación para la función "print" en Python, en realidad podemos mirar esta URL aquí: docs.python.org es donde se encuentra toda la documentación oficial de Python.

Si busco más detalladamente, puedo encontrar mi camino hacia esta URL más específicamente, donde puedo encontrar todas las funciones disponibles en Python y su documentación. Y si busco un poco más precisamente, incluso puedo encontrar documentación específica para la función "print" en sí. Y en lugar de abrir eso en un navegador, voy a resaltar solo una línea de esa misma URL, que es esta, y esto es fácilmente lo más críptico que hemos visto hasta ahora, pero esta es la documentación oficial para la función "print". Y una de las mejores cosas que puedes hacer al aprender un lenguaje de programación es, sinceramente, aprender a leer la documentación, porque realmente todas las respuestas a tus preguntas estarán de alguna manera allí, aunque, admítelo, no siempre es obvio. Y también diré que la documentación de Python no es necesariamente lo más fácil, especialmente para un programador principiante o de primera vez. También requiere práctica, así que trata de no abrumarte si no estás seguro de lo que estás viendo.

Pero déjame guiarte en este ejemplo. Esto nuevamente es una línea de texto de la documentación oficial de Python para la función "print". Lo que esto indica es lo siguiente: el nombre de esta función es, por supuesto, "print". Luego hay un paréntesis aquí y otro paréntesis cerrado allá. Todo lo que está dentro de esos paréntesis son los argumentos, los posibles argumentos, de la función. Sin embargo, cuando estamos viendo estos argumentos en la documentación de esta manera, técnicamente hay un término diferente que usaríamos. Técnicamente, estos son los parámetros de la función. Entonces, cuando hablamos de lo que se puede pasar a una función y cómo se llaman esos valores, esos son parámetros. Cuando realmente usas la función y pasas valores dentro de esos paréntesis, esos valores son argumentos. Por lo tanto, estamos hablando de exactamente lo mismo: parámetros y argumentos son efectivamente lo mismo, pero los términos que se usan varían dependiendo de cómo se mire el problema desde diferentes direcciones. Cuando estamos viendo qué puede aceptar la función versus lo que realmente estás pasando a la función.

Entonces, ¿qué implica esto? Bueno, esta sintaxis es bastante críptica, pero por el momento, solo debes saber que un asterisco, una estrella, y luego la palabra "objetos" significa que la función "print" puede aceptar cualquier número de objetos. Puedes pasarle 0 cadenas de texto, una cadena de texto como hice yo, dos cadenas de texto como hice yo, o, técnicamente, un número infinito si realmente lo deseas, aunque ese código no se verá muy bien. Después de eso, vemos una coma, luego vemos otro parámetro llamado SEP, abreviatura de separador en inglés. Y observa el signo igual y la comilla simple, espacio, comilla simple. Entonces, 'espacio' no sé qué es todavía, pero creo que hemos visto una pista al respecto.

Centrémonos por un momento en esto: la función "print" toma otro parámetro llamado END y el valor predeterminado de ese parámetro es aparentemente, según este signo igual y estas comillas, \n. Y ¿qué es \n, si te gustaría comentar en el chat? Cualquiera que haya programado antes probablemente haya visto esto, aunque si nunca has programado, esto puede parecer bastante críptico. "\n" significa nueva línea, y es una forma de indicar textualmente si y cuándo quieres que la computadora efectivamente mueva el cursor a la siguiente línea, crear una nueva línea de texto. Y técnicamente, si leemos la documentación, veremos más detalles al respecto. El hecho de que haya un parámetro llamado END en la documentación para la función "print" simplemente significa que de forma predeterminada, esta función "print" va a terminar cada línea con \n. No ves literalmente \n, ves una nueva línea. Ves el cursor moviéndose a la siguiente línea.

Ahora, siguiendo esa lógica, retrocedamos. SEP para separador: el valor predeterminado del separador es aparentemente un solo espacio en blanco. Bueno, ¿dónde hemos visto eso? Bueno, recuerda que en un ejemplo anterior cuando pasé no solo un sino dos argumentos a la función "print". Recuerda que mágicamente tenían un espacio entre ellos. De hecho, tenían ese espacio + mi propio espacio y por eso eliminé mi espacio porque en ese momento era innecesario. Entonces, esto simplemente significa que cuando pasas múltiples argumentos a "print", de forma predeterminada estarán separados por un solo espacio. De forma predeterminada, cuando pasas argumentos a "print", todo se termina con una nueva línea.

Ahora, solo por saber esto, y permíteme literalmente hacer un gesto con la mano al resto de la documentación para otro día, hay más cosas que "print" puede hacer, pero nos vamos a centrar solo en SEP y en END, veamos si no podemos aprovechar esto ahora para resolver ese problema original. El problema original fue este, no me gusta que "Hello, David" esté en dos líneas diferentes. Bueno, eso está sucediendo nuevamente porque "print" está imprimiendo automáticamente una nueva línea, así que digámosle que no lo haga. Digámosle pasando un segundo argumento a la primera instancia de PRINT para decir END = comillas, sin nada más. Anulemos el valor predeterminado para que no haya una nueva línea. Literalmente, no haya nada ahí. Y veamos qué sucede.

Bajemos a la ventana de la terminal y la borremos. Y voy a ejecutar Python con Hello.py, Enter. Voy a escribir mi nombre, David, y creo que ahora todo se va a mantener en la misma línea porque... y lo hizo... esta línea aquí, 5, va a imprimir "Hello, coma, espacio", pero luego no hay nada al final porque lo cambié a ser comillas. La segunda línea va a imprimir el nombre, David, o cualquier nombre humano, y moverá el cursor a la siguiente línea porque no cambié el valor de END allí.

Solo para ver esto de manera más explícita, si haces algo críptico como, bueno, no tengo idea de lo que está pasando. Permíteme poner temporalmente tres signos de interrogación aquí. Veremos los resultados de esto también. Bajemos ahora a la ventana de la terminal, ejecutemos Python con hello.py, ¿cuál es tu nombre? David. Y ahora ves una salida realmente fea, pero ves literalmente qué está pasando. "Hello, coma, espacio", luego tres signos de interrogación, END, esa declaración de "print", y luego ves D-A-V-I-D. Así que no es un buen resultado, pero demuestra cuánto control tenemos aquí también.

Y volvamos atrás más. Recuerda que en nuestra otra versión de esto, cuando pasé "Hello, coma" y el nombre, estaban separados por un solo espacio. Entonces, Python con Hello.py, D-A-V-I-D, Enter. Eso simplemente funcionó. Bueno, ¿qué pasa si anulamos el valor de SEP para separador? En lugar de ser un solo espacio, podríamos decir algo como, interrogante, interrogante, interrogante, solo para entender qué está pasando. Ahora, hagamos Python con Hello.py, D-A-V-I-D, Enter. Y ahora ves dos. Estos dos inputs: "Hello, coma" y el nombre ahora están separados de manera fea por tres signos de interrogación porque he anulado el comportamiento predeterminado de SEP.

Y aunque la documentación usa comillas simples, he estado usando comillas dobles por hábito. En Python puedes usar cualquiera. Estrictamente hablando, no importa, pero debes ser consistente y generalmente siempre uso comillas dobles. Sin embargo, la documentación de Python siempre utiliza comillas simples. ¿Alguna pregunta sobre estos tipos de parámetros? Permíteme proponer que les demos un nombre oficial. Hasta ahora, cuando hemos estado pasando valores a "print", se llaman parámetros posicionales: posicionales en el sentido de que lo primero que pasas a "print" se imprime primero. Lo segundo que pasas a "print" después de una coma se imprime en segundo lugar. Y así sucesivamente. Pero también hay estas cosas que ahora hemos visto llamadas parámetros con nombre. SEP, separador, o END, E-N-D para el final de línea. Esos son parámetros con nombre porque uno, son opcionales y puedes pasarlos al final de tu instrucción de "print", pero también puedes usarlos por nombre.