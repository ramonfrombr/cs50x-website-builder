Muy bien, esto es mucho de una vez: esta es una introducción a la programación orientada a objetos. Pero podría ser bastante sorprendente que, aunque hayamos identificado la POO por su nombre en semanas anteriores, todos hemos estado utilizando clases y objetos durante semanas en esta clase. De hecho, si piensas en una de las primeras cosas que hicimos en esta clase, usamos enteros y simplemente obtuvimos enteros del usuario. Pero si aún no lo has hecho, si vas y buscas en la documentación de los enteros, que, nuevamente, se encuentra en esta URL aquí, en realidad encontrarías que int en sí mismo es y ha sido durante semanas una clase. De hecho, esta es la firma de la llamada al constructor para un int, donde pasas x, como un número, entre comillas, "50" o algo más, pasas opcionalmente la base, 10 para decimal, 2 para binario o cualquier otra cosa. Y esa función int en realidad te devolverá, todo este tiempo, un objeto de tipo int. Es decir, int es una clase. Es una plantilla, un modelo para crear enteros en la memoria. Y cada vez que tú y yo hemos convertido una cadena, por ejemplo, en un int, tú y yo hemos estado creando un objeto de tipo int que estaba llamando, aparentemente, al método __init__ que alguien más, los autores de Python, escribieron para devolvernos ese entero apropiado. Además de eso, si puedes creerlo, las strs, las cadenas en Python, también han sido clases desde la primera semana de esta clase. Si buscas en la documentación de una str, que se encuentra en una URL similar allí, encontrarás que, al instanciar, es decir, crear una str, toma opcionalmente un parámetro llamado objeto aquí, cuyo valor predeterminado es simplemente una cadena vacía, una cadena en blanco, por así decirlo. Pero cada vez que tú y yo hemos creado strs o incluso hemos utilizado explícitamente la función str, estás obteniendo de vuelta un objeto de tipo str. Cada vez que tú y yo hemos forzado a una cadena a minúsculas según la documentación, usando la sintaxis como esta, tú y yo hemos estado tomando un objeto de tipo str y forzándolo todo a minúsculas llamando a un método llamado lower, un método que los autores de Python incorporaron a la clase str, pero ha estado allí desde el principio, por lo que esta noción de métodos ni siquiera es nueva hoy. No estarías haciéndolo durante tanto tiempo. Si alguna vez has llamado a strip para eliminar los espacios en blanco al comienzo y al final de una cadena en Python, estás llamando a otro método que viene con Python, escrito por los autores de Python. Y aunque en ese momento no lo llamamos una clase, una str, todo este tiempo ha sido una clase. Y las instancias de cadenas son, en sí mismas, objetos. Y esos objetos vienen, por lo tanto, con estas funciones incorporadas, también conocidas como métodos, que nos permiten hacer cosas como forzar a minúsculas y eliminar los espacios en blanco del principio y del final. Hagamos otro ejemplo. list: cada vez que has creado una lista, ya sea sintácticamente con corchetes cuadrados o literalmente con L-I-S-T, paréntesis abierto, paréntesis cerrado, lo has estado haciendo usando una clase. Si vas a la documentación de list, en esta URL similar aquí, o más específicamente, al tutorial sobre listas aquí en Python, verás que una lista es y ha sido, desde las primeras semanas de esta clase, una clase en sí misma. Y esa clase de lista toma, como parte de su inicialización, un iterable opcional, algo que se puede iterar, como 1, 2, 3 o alguna lista de valores, y luego puedes obtener una lista que contiene esos mismos valores iterables. Si alguna vez has agregado algo a una lista en esta clase, como yo mismo lo he hecho en el pasado, has estado usando un método llamado append que viene con la clase list, que, según la x aquí, toma un argumento que te permite agregar algo a la lista actual, también conocida como self en el contexto de ese método. Podemos hacer esto todo el día. Si has utilizado un diccionario o un dict en Python, en realidad, todo este tiempo, los he estado llamando objetos dict, y eso es por una razón. dict en sí es una clase en Python, si miras su documentación oficial aquí. Y verás que está definido, de hecho, como una clase. Y esa clase también viene con métodos. Y, por lo tanto, cada vez que hemos manipulado diccionarios, hemos estado usando, bajo el capó, todos esos mismos métodos. Y de hecho, podemos ver esto si estamos realmente interesados. Permíteme ir aquí a VS Code. Y permíteme crear un nuevo archivo que, de manera muy simple, haga algo de juego con los tipos de datos. Y permíteme crear un nuevo archivo, por ejemplo, llamado type.py, solo para poder investigar dentro de algunos valores. Y en type.py, simplemente voy a hacer lo siguiente. Voy a imprimir cualquiera que sea el tipo del número 50. Y esta es una función que quizás no me hayas visto usar antes, y no es una función que usarías frecuentemente en tu propio código. Hay otras formas de detectar, si es necesario, cuál es el tipo de una variable. Pero en este caso, el tipo de 50 solo va a decirme y luego imprimir cuál es el tipo de datos de ese valor. Ahora, con suerte, todos podríamos adivinar que 50 es, de hecho, un entero, es decir, un int, pero podemos verlo de esta manera. Y esto es lo que es poderoso de conocer un poco de programación. Si quieres saber la respuesta a una pregunta, simplemente pruébalo, como estoy haciendo aquí. Así que permíteme ejecutar Python de type.py, Enter. Y ahí está. Cuando imprimes el tipo del número 50, verás en la pantalla, en esta sintaxis críptica, class 'int'. Esto no es algo que probablemente quieras mostrar al usuario. Pero si tú mismo quieres investigar y ver qué está pasando o tal vez usar esa información de alguna manera, ciertamente tienes a tu disposición usar esta función type para eso. Cambiémoslo un poco. En lugar de pasar como argumento a type 50, como un int, escribamos algo también familiar como "hola, mundo", entre comillas dobles o simples. Permíteme volver a mi ventana de terminal, limpiar la pantalla y ejecutar Python de type.py nuevamente. Y ahora, voilà, ahí está. Todo este tiempo, un str también es una clase. Podemos hacer esto algunas veces más. Por ejemplo, cambiemos "hola, mundo" a solo una lista vacía, corchete abierto, corchete cerrado. Y esto comienza a verse un poco críptico, pero nuevamente, nota lo que estoy haciendo. En corchetes cuadrados hay una lista vacía. Ya hemos hecho eso antes. Ese es el único argumento de esta nueva función type. Y eso solo se pasa a la función print para que el valor devuelto de type sea el argumento de print. Así que si ahora ejecuto este código, Python de type.py, ahí está. Una lista también es una clase. Puedes recordar que dije que también puedes crear una lista vacía simplemente haciendo list (). Esto es un poco inconsistente, ya que ahora podemos identificar que int y str y ahora list - en realidad, todos están en minúsculas. Y me esforcé mucho en crear mi clase Student para que tuviera esa S mayúscula. Esa es una convención. Debido a que int, str y list y otros vienen con Python, decidieron hacer que sus tipos de datos incorporados -aunque sean clases- se escriban en minúscula. Pero la convención, la recomendación en la comunidad de Python al crear tus clases es comenzar con mayúscula la primera letra, como hice en Student, con S mayúscula. Pero list () es igual a simplemente dos corchetes vacíos. Si limpio mi pantalla y ejecuto type.py nuevamente, verás exactamente lo mismo. La clase se llama list. Hagamos uno más. Cambiemos la lista para que no sean corchetes, sino llaves. Ya hemos hecho esto antes. Cada vez que he utilizado dos llaves sin nada en medio, esto, por supuesto, es un diccionario vacío o un objeto dict en Python. Bueno, ahora podemos ver eso. Permíteme limpiar mi pantalla, ejecutar Python de type.py, Enter, y ahí está, class 'dict'. Ha estado allí todo este tiempo. Simplemente no lo llamamos clase hasta hoy. Puedo hacer esto de manera similar de manera explícita. En lugar de dos llaves, escribamos dict con dos paréntesis. Ahora tenemos muchos paréntesis nuevamente, al igual que con la lista. Pero esto simplemente deja aún más claro que el tipo de un objeto dict es, de hecho, la clase dict en sí misma. Esto quiere decir que, por más nuevas que sean muchas de las ideas y sintaxis de hoy, de hecho las has estado utilizando, tal vez sin saberlo, durante semanas. Ahora simplemente tenemos terminología para describir lo que hemos estado haciendo todo este tiempo. Y ahora tienes la expresividad, con un poco de práctica, para crear tus propias clases, dentro de las cuales están tus propias variables de instancia, tal vez envueltas con esas propiedades y tus propios métodos de instancia.