Entonces, vamos a seguir adelante y hacer esto. Comencemos escribiendo un programa de manera muy procedural abriendo VS Code aquí. Voy a crear un programa llamado student.py. Y en este programa, quiero hacer algo relativamente simple al principio, como podríamos haber hecho hace algunas semanas, donde simplemente le pido al usuario su nombre y, tal vez en el contexto del universo de Harry Potter, su casa, y simplemente imprimo de dónde es ese estudiante. Y vamos a ir mejorando gradualmente este programa agregando más y más características y ver si nos encontramos con problemas que, hasta ahora, podríamos no haber tenido soluciones elegantes y bien diseñadas. Pero si introducimos, de manera explícita, la programación orientada a objetos como una técnica de programación, apuesto a que podemos limpiar nuestro código y preparar el escenario para escribir programas aún más sofisticados, programas más largos en el futuro. Entonces, en student.py, voy a declarar una variable llamada name y asignarle el valor de la función input, para pedir al usuario su nombre así. Y luego voy a hacer lo mismo para una variable house y pedir al usuario su casa, usando input así. Y hagamos algo muy simple ahora. Vamos a imprimir una cadena formateada que diga algo como "name from house", solo para confirmar que el contenido de estas variables es lo que espero. Por ahora, no voy a hacer ninguna verificación de errores o recorte ni nada por el estilo. Voy a imprimir lo que el usuario acaba de escribir. Muy bien, vamos a ejecutar Python student.py. Digamos "Harry", como en Harry Potter, de Gryffindor. Y cuando presione Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, creo que tenemos un programa funcional en este punto, pero ahora vamos a introducir algunas lecciones aprendidas desde la semana cero, donde comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema más correctamente; diría que esto es correcto tal como está. Pero comienzan a brindarnos bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabamos de escribir dentro de nuestro método típico llamado "main", e indentemos esas tres líneas para que al menos estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, asumamos, por el momento, que tenemos una función llamada "get_name" en el mundo, y supongamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida será, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, ahora tengo que implementar estas funciones. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No se le pasa nada entre paréntesis porque no tomará un parámetro. Y voy a hacer algo como "name = input('Name')", justo como antes, y luego voy a retornar "name". Así que es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" cuyos detalles de implementación ya no tengo que preocuparme. Solo sé que la función existe. Y puedo mejorar esto aún más. No necesito una variable "name" en la línea 8 si voy a retornar inmediatamente esa misma variable "name" en la línea 9. Así que voy a simplificar esto un poco aunque no cambie la funcionalidad y simplemente retornar inmediatamente el valor de retorno de la llamada a la función "input" aquí. Vamos a hacer algo muy similar ahora para "get_house", que también tomará argumentos. Voy a retornar el valor de retorno de "input", esta vez pidiéndole al usuario su casa. Y necesito un detalle final. En la parte inferior, vamos a continuar con nuestro hábito de hacer esto si el nombre de este archivo es igual a "main", entonces vamos a llamar realmente a "main" y recordemos que esto está en su lugar para que, si eventualmente esto se convierte en parte de un módulo, una biblioteca de algún tipo, no llame accidentalmente a "main" a ciegas. Solo lo hago si quiero ejecutar "main" desde la línea de comandos en este archivo. Muy bien, si no cometí ningún error aquí, déjame ir a mi ventana de terminal, nuevamente ejecutar "Python student.py". Escribe "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero comienza a darnos bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main" y voy a indentar esas tres líneas para que al menos estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos, por el momento, que tenemos una función llamada "get_name" en el mundo y supongamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida es, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No habrá nada entre los paréntesis porque no tomará un parámetro. Y voy a hacer algo como "name = input ("Name")", exactamente como antes, y luego voy a retornar "name". Entonces, es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" cuyos detalles de implementación ya no tengo que preocuparme. Solo sé que la función existe. Y puedo solucionar esto incluso más. Realmente no necesito una variable "name" en la línea 8 si inmediatamente voy a retornar esa misma variable "name" en la línea 9. Entonces, voy a simplificar esto un poco aunque no cambie la funcionalidad y simplemente retornar inmediatamente el valor de retorno de la llamada a la función "input" aquí. Haremos algo muy similar ahora para "get_house", que también no tomará argumentos. Voy a retornar el valor de retorno de "input", esta vez pidiendo al usuario su casa. Y necesito un detalle final. En la parte inferior, continuemos con nuestro hábito de hacer esto if el nombre de este archivo es igual a "main", entonces sigamos adelante y llamemos a "main" y recuerda que esto está aquí para que, si esto se convierte en parte de un módulo, una biblioteca, no llame a "main" ciegamente. Solo lo hago si pretendo ejecutar "main" desde la línea de comandos en este archivo. Muy bien, si no cometí ningún error aquí, déjame ir a mi ventana de terminal, nuevamente ejecutar Python de student.py. Escribe "Harry" como en Harry Potter, de Gryffindor. Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero comenzamos a tener bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main", e indentemos esas tres líneas para que al menos estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos, por ahora, que tenemos una función llamada "get_name" en el mundo, y supongamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida es, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No hay nada en estos paréntesis porque no tomará un parámetro. Y voy a hacer algo como "name = input('Name')", justo como antes, y luego voy a devolver "name". Así que es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" cuyos detalles de implementación ya no tengo que preocuparme. Solo sé que la función existe. Y puedo mejorar esto aún más. Realmente no necesito una variable "name" en la línea 8 si voy a devolver inmediatamente esa misma variable "name" en la línea 9. Entonces, voy a apretar esto un poco, aunque no cambie la funcionalidad y simplemente devolver inmediatamente el valor de retorno de la llamada a la función "input" aquí. Hagamos algo muy similar ahora para "get_house", que tampoco tomará argumentos. Voy a devolver el valor de retorno de "input", esta vez pidiéndole al usuario su casa. Y necesitan un detalle final. En la parte inferior, continúen con nuestro hábito de hacer esto si el nombre de este archivo es igual a "main", entonces vamos a realmente llamar a "main" y recordemos que esto está aquí para que, si esto se convierte eventualmente en parte de un módulo, una biblioteca de algún tipo, no llamemos a "main" ciegamente. Lo hago solo si quiero ejecutar "main" desde la línea de comandos en este archivo. Muy bien, entonces si no cometí ningún error aquí, déjame ir a mi ventana de terminal, nuevamente ejecutar Python de student.py. Escribamos "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero comenzamos a tener bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main" y voy a indentar esas tres líneas para que, al menos, estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos, por ahora, que tenemos una función llamada "get_name" en el mundo y supongamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida es, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No hay nada entre los paréntesis porque no tomará un parámetro. Y voy a hacer algo como "nombre = input ('Nombre')", exactamente como antes, y luego voy a retornar "nombre". Entonces, es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" cuyos detalles de implementación no tengo que preocuparme. Solo sé que la función existe. Y puedo apretar esto aún más. En realidad, no necesito una variable "nombre" en la línea 8 si de inmediato voy a retornar esa misma variable "nombre" en la línea 9. Entonces, voy a apretar esto un poco aunque no cambie la funcionalidad y simplemente retornar inmediatamente el valor de retorno de la llamada a la función "input" aquí. Vamos a hacer algo muy similar ahora para "get_house", que tampoco tomará parámetros. Voy a retornar el valor de retorno de "input", esta vez pidiendo al usuario su casa. Y necesito un detalle final. En la parte inferior, continuemos con nuestro hábito de hacer esto. Si el nombre de este archivo es igual a "main", entonces vamos a realmente llamar a "main" y recordemos que esto está aquí para que, si esto se convierte en parte de un módulo, una biblioteca de algún tipo, no llamaremos a "main" ciegamente. Lo hago solo si pretendo ejecutar "main" desde la línea de comandos en este archivo. Muy bien, así que si no cometí ningún error aquí, déjame ir a mi ventana de terminal, nuevamente ejecutar Python student.py. Escribamos "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero comenzamos a tener bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main" y voy a indentar esas tres líneas para que, al menos, estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos que tenemos una función llamada "get_name" en el mundo y supongamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida es, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No hay nada en estos paréntesis porque no tomará un parámetro. Y voy a hacer algo como "nombre = input ('Nombre')", exactamente como antes, y luego voy a devolver "nombre". Entonces, es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" de la cual no tengo que preocuparme por los detalles de implementación. Solo sé que la función existe. Y estoy apretando esto aún más. De hecho, no necesito una variable "nombre" en la línea 8 si de inmediato voy a devolver esa misma variable "nombre" en la línea 9. Entonces, voy a apretar esto un poco aunque no cambie la funcionalidad y simplemente devolver inmediatamente el valor de retorno de la llamada a la función "input" aquí. Vamos a hacer algo muy similar ahora para "get_house", que tampoco tomará parámetros. Voy a devolver el valor de retorno de "input", esta vez pidiendo al usuario su casa. Y necesito un detalle final. En la parte inferior, continuemos con nuestro hábito de hacer esto si el nombre de este archivo es igual a "main", entonces vamos a realmente llamar a "main" y recordemos que esto está aquí para que, si esto se convierte eventualmente en parte de un módulo, una biblioteca de algún tipo, no llamemos a "main" ciegamente. Lo hago solo si quiero ejecutar "main" desde la línea de comandos en este archivo. Muy bien, entonces si no cometí ningún error aquí, déjame ir a mi ventana de terminal, nuevamente ejecutar "Python student.py". Escriba "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero comenzamos a tener bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main" y voy a indentar esas tres líneas para que, al menos, estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos, por ahora, que tenemos una función llamada "get_name" en el mundo y supongamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida es, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No hay nada en estos paréntesis porque no tomará un parámetro. Y voy a hacer algo como "nombre = input ('Nombre')", exactamente como antes, y luego voy a devolver "nombre". Entonces, es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" de la cual no tengo que preocuparme por los detalles de implementación. Solo sé que la función existe. Y puedo apretar esto aún más. En realidad, no necesito una variable "nombre" en la línea 8 si voy a devolver inmediatamente esa misma variable "nombre" en la línea 9. Entonces, voy a apretar esto un poco aunque no cambie la funcionalidad y simplemente devolver inmediatamente el valor de retorno de la llamada a la función "input" aquí. Vamos a hacer algo muy similar ahora para "get_house", que tampoco tomará parámetros. Voy a devolver el valor de retorno de "input", esta vez pidiendo al usuario su casa. Y necesito un detalle final. En la parte inferior, continuemos con nuestro hábito de hacer esto. Si el nombre de este archivo es igual a "main", entonces vamos a realmente llamar a "main" y recordemos que esto está aquí para que, si esto se convierte eventualmente en parte de un módulo, una biblioteca de algún tipo, no llamemos a "main" ciegamente. Lo hago solo si quiero ejecutar "main" desde la línea de comandos en este archivo. Muy bien, entonces si no cometí ningún error aquí, déjame ir a mi ventana de terminal, nuevamente ejecutar "Python student.py". Escriba "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero comenzamos a tener bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main" y voy a indentar esas tres líneas para que, al menos, estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos, por ahora, que tenemos una función llamada "get_name" en el mundo, y supongamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida es, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No hay nada en estos paréntesis porque no tomará un parámetro. Y voy a hacer algo como "nombre = input ('Nombre')", exactamente como antes, y luego voy a devolver "nombre". Entonces, es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" de la cual no tengo que preocuparme por los detalles de implementación. Solo sé que la función existe. Y puedo apretar esto aún más. En realidad, no necesito una variable "nombre" en la línea 8 si voy a devolver inmediatamente esa misma variable "nombre" en la línea 9. Entonces, voy a apretar esto un poco aunque no cambie la funcionalidad y simplemente devolver inmediatamente el valor de retorno de la llamada a la función "input" aquí. Vamos a hacer algo muy similar ahora para "get_house", que tampoco tomará parámetros. Voy a devolver el valor de retorno de "input", esta vez pidiéndole al usuario su casa. Y necesito un detalle final. En la parte inferior, continuemos con nuestro hábito de hacer esto. Si el nombre de este archivo es igual a "main", entonces vamos a realmente llamar a "main" y recordemos que esto está aquí para que, si esto se convierte eventualmente en parte de un módulo, una biblioteca de algún tipo, no llamemos a "main" ciegamente. Lo hago solo si quiero ejecutar "main" desde la línea de comandos en este archivo. Muy bien, entonces si no cometí ningún error aquí, déjame ir a mi ventana de terminal, nuevamente ejecutar "Python student.py". Escriba "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero comenzamos a tener bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main" y voy a indentar esas tres líneas para que al menos estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos por ahora que tenemos una función llamada "get_name" en el mundo y supongamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida es, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No toma ningún parámetro entre los paréntesis porque no tomará ningún parámetro. Y voy a hacer algo como "nombre = input ('Nombre'), exactamente como antes. Y luego voy a devolver "nombre". Entonces, es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" de la cual no tengo que preocuparme por los detalles de implementación. Solo sé que la función existe. Y puedo apretar esto aún más. En realidad, no necesito una variable "nombre" en la línea 8 si voy a devolver inmediatamente la misma variable "nombre" en la línea 9. Entonces, voy a apretar esto un poco aunque no cambie la funcionalidad. Simplemente voy a devolver inmediatamente el valor de retorno de la llamada a la función "input" aquí. Hagamos algo muy similar ahora para "get_house", que tampoco tomará parámetros. Voy a devolver el valor de retorno de "input", esta vez pidiendo al usuario su casa. Y necesito un último detalle. Al final, continuemos con nuestro hábito de hacer esto. Si el nombre de este archivo es igual a "main", entonces llamemos a "main", y recuerda que esto está aquí para que, si esto se convierte en parte de un módulo, una biblioteca de algún tipo, no llamemos a "main" ciegamente. Solo lo hago si quiero ejecutar "main" desde la línea de comandos en este archivo. Muy bien, así que si no cometí ningún error aquí, déjame ir a mi ventana de la terminal, nuevamente ejecutar "Python student.py". Escribe "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero comenzamos a tener bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main" y voy a indentar esas tres líneas para que al menos estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos por ahora que tenemos una función llamada "get_name" en el mundo y supongamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida es, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No toma ningún parámetro entre los paréntesis porque no tomará ningún parámetro. Y voy a hacer algo como "nombre = input('Nombre'), exactamente como antes. Y luego voy a devolver "nombre"; Entonces, es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" de la cual no tengo que preocuparme por los detalles de implementación. Solo sé que la función existe. Y puedo apretar esto aún más. En realidad, no necesito una variable "nombre" en la línea 8 si voy a devolver inmediatamente la misma variable "nombre" en la línea 9. Entonces, voy a hacer esto un poco más breve aunque no cambie la funcionalidad. Simplemente voy a devolver inmediatamente el valor de retorno de la llamada a la función "input" aquí. Hagamos algo muy similar ahora en "get_house", que tampoco tomará parámetros. Voy a devolver el valor de retorno de "input", esta vez pidiendo al usuario su casa. Y necesito un último detalle. Al final, sigamos con nuestro hábito de hacer esto. Si el nombre de este archivo es igual a "main", entonces llamemos a "main" y recuerda que esto está aquí para que, si esto se convierte eventualmente en parte de un módulo, una biblioteca de algún tipo, no llamemos a "main" ciegamente. Solo lo hago si quiero ejecutar "main" desde la línea de comandos en este archivo. Muy bien, entonces, si no cometí ningún error aquí, déjame ir a mi ventana de la terminal, nuevamente ejecutar "Python student.py". Escribamos "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero comenzamos a tener bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main" y voy a indentar estas tres líneas para que al menos estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos por ahora que tenemos una función llamada "get_name" en el mundo y supongamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida es, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No toma ningún parámetro entre los paréntesis porque no tomará ningún parámetro. Y voy a hacer algo como "nombre = input('Nombre'), exactamente como antes. Y luego voy a devolver "nombre". Entonces, es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" de la cual no tengo que preocuparme por los detalles de implementación. Solo sé que la función existe. Y puedo apretar esto aún más. En realidad, no necesito una variable "nombre" en la línea 8 si voy a devolver inmediatamente la misma variable "nombre" en la línea 9. Entonces, voy a hacer esto un poco más breve aunque no cambie la funcionalidad. Simplemente voy a devolver inmediatamente el valor de retorno de la llamada a la función "input" aquí. Hagamos algo muy similar ahora en "get_house", que tampoco tomará parámetros. Voy a devolver el valor de retorno de "input", esta vez pidiendo al usuario su casa. Y necesito un último detalle. Al final, sigamos con nuestro hábito de hacer esto. Si el nombre de este archivo es igual a "main", entonces llamemos a "main", y recuerda que esto está aquí para que, si esto se convierte eventualmente en parte de un módulo, una biblioteca de algún tipo, no llamemos a "main" ciegamente. Solo lo hago si quiero ejecutar "main" desde la línea de comandos en este archivo. Muy bien, entonces, si no cometí ningún error aquí, déjame ir a mi ventana de la terminal, nuevamente ejecutar "Python student.py". Escribamos "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero ahora estamos teniendo bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main", e indentar estas tres líneas para que al menos estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos, por ahora, que tenemos una función llamada "get_name" en el mundo, y asumamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida será, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, vayamos más abajo en mi archivo y definamos una función llamada "get_name". No toma ningún parámetro entre los paréntesis porque no tomará ningún parámetro. Y voy a hacer algo como "nombre = input('Nombre')", justo como antes, y luego voy a devolver "nombre". Entonces, es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name" de la cual no tengo que preocuparme por los detalles de implementación. Solo sé que la función existe. Y puedo apretar esto aún más. En realidad, no necesito una variable "nombre" en la línea 8 si voy a devolver inmediatamente la misma variable "nombre" en la línea 9. Entonces, voy a hacer esto un poco más breve, aunque no cambie la funcionalidad. Simplemente voy a devolver inmediatamente el valor de retorno de la llamada a la función "input" aquí. Hagamos algo muy similar ahora en "get_house", que tampoco tomará parámetros. Voy a devolver el valor de retorno de "input", esta vez pidiendo al usuario su casa. Y necesito un último detalle. Al final, sigamos con nuestro hábito de hacer esto. Si el nombre de este archivo es igual a "main", entonces vamos a realmente llamar a "main", y recuerda que esto está aquí para que, si esto se convierte eventualmente en parte de un módulo, una biblioteca de algún tipo, no llamemos "main" ciegamente. Solo lo hago si quiero ejecutar "main" desde la línea de comandos en este archivo. Muy bien, entonces, si no cometí ningún error aquí, déjame ir a mi ventana de la terminal, nuevamente ejecutar "Python student.py". Escribamos "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero ahora tenemos bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main", e indentar esas tres líneas para que al menos estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos por ahora que tenemos una función llamada "get_name" en el mundo, y asumamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida será, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Así que bajemos más en mi archivo y definamos una función llamada "get_name". No toma ningún parámetro entre los paréntesis porque no tomará ningún parámetro. Y voy a hacer algo como "nombre = input('Nombre')", exactamente como antes, y luego voy a devolver "nombre". Entonces, es una función muy simple, pero es una abstracción. Ahora tengo una función llamada "get_name", cuyos detalles de implementación ya no tengo que preocuparme. Solo sé que la función existe. Y puedo apretar esto aún más. En realidad, no necesito una variable "nombre" en la línea 8 si voy a devolver inmediatamente la misma variable "nombre" en la línea 9. Entonces, voy a hacer esto un poco más breve, aunque no cambie la funcionalidad. Simplemente voy a devolver inmediatamente el valor de retorno de la llamada a la función "input" aquí. Vamos a hacer algo muy similar ahora para "get_house", que tampoco tomará parámetros. Voy a devolver el valor de retorno de "input", esta vez pidiendo al usuario su casa. Y necesito un último detalle. Sigamos con nuestro hábito de hacer esto al final. Si el nombre de este archivo es igual a "main", entonces realmente llamemos a "main", y recuerda que esto está aquí para que, si esto se convierte eventualmente en parte de un módulo, una biblioteca de algún tipo, no llamemos a "main" ciegamente. Solo lo hago si quiero ejecutar "main" desde la línea de comandos en este archivo. Muy bien, entonces si no cometí ningún error aquí, permíteme ir a mi ventana de la terminal, nuevamente ejecutar "Python student.py". Escribamos "Harry" como en Harry Potter y "Gryffindor". Y cuando presiono Enter, veamos si veo que Harry de Gryffindor es realmente el caso. Muy bien, todavía tenemos un código funcional en este punto, pero ahora vamos a introducir algunas de esas lecciones aprendidas desde la semana cero, cuando comenzamos a escribir nuestras propias funciones, no necesariamente porque resuelvan el problema de manera más correcta. Me atrevería a decir que esto es correcto tal como está. Pero ahora tenemos bloques de construcción que podemos ampliar para resolver programas más complicados. Entonces, volvamos a student.py y hagamos esto. Vamos a poner toda la lógica que acabo de escribir dentro de nuestro método típico llamado "main", e indentar esas tres líneas para que al menos estén combinadas en un solo método principal. Pero en lugar de usar "input" en la línea 2 y "input" en la línea 3, supongamos, por ahora, que tenemos una función llamada "get_name" en el mundo, y asumamos que tenemos otra función llamada "get_house" en el mundo que no toman parámetros. Pero su propósito en la vida será, según su nombre, obtener el nombre del usuario y obtener su casa, respectivamente. Y luego voy a imprimir la misma cadena formateada que antes. Por supuesto, necesito implementar estas funciones ahora. Entonces, bajemos más en mi archivo y definamos una función llamada "get_name". No toma ningún parámetro entre los paréntesis porq