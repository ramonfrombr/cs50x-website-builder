# Clase 5

-   [Pruebas Unitarias](#unit-tests)
-   [`assert`](#assert)
-   [`pytest`](#pytest)
-   [Pruebas de Cadenas](#testing-strings)
-   [Organización de Pruebas en Carpetas](#organizing-tests-into-folders)
-   [Resumen](#summing-up)

## Pruebas Unitarias <a id="unit-tests"></a>

-   Hasta ahora, es probable que hayas estado probando tu propio código utilizando declaraciones `print`.
-   Alternativamente, es posible que hayas estado confiando en CS50 para probar tu código por ti.
-   En la industria es más común escribir código para probar tus propios programas.
-   En la ventana de la consola, escribe `code calculator.py`. Ten en cuenta que probablemente hayas codificado este archivo en una conferencia anterior. En el editor de texto, asegúrate de que tu código aparezca de la siguiente manera:

        def main():
            x = int(input("¿Cuál es el valor de x? "))
            print("El cuadrado de x es", square(x))


        def square(n):
            return n * n


        if __name__ == "__main__":
            main()

    Observa que podrías probar el código anterior por tu cuenta utilizando algunos números obvios como `2`. Sin embargo, piensa en por qué podrías querer crear una prueba que asegure que el código anterior funcione adecuadamente.

-   Siguiendo la convención, creemos un nuevo programa de prueba escribiendo `code test_calculator.py` y modificando tu código en el editor de texto de la siguiente manera:

        from calculator import square


        def main():
            test_square()


        def test_square():
            if square(2) != 4:
                print("El cuadrado de 2 no fue 4")
            if square(3) != 9:
                print("El cuadrado de 3 no fue 9")


        if __name__ == "__main__":
            main()

    Observa que estamos importando la función `square` desde `calculator.py` en la primera línea de código. Por convención, estamos creando una función llamada `test_square`. Dentro de esa función, definimos algunas condiciones para probar.

-   En la ventana de la consola, escribe `python test_calculator.py`. Notarás que no se está mostrando nada. ¡Podría ser que todo esté funcionando bien! De lo contrario, quizás nuestra función de prueba no descubrió uno de los "casos límite" que podrían producir un error.
-   En este momento, nuestro código prueba dos condiciones. Si quisiéramos probar muchas más condiciones, nuestro código de prueba podría volverse fácilmente abultado. ¿Cómo podríamos ampliar nuestras capacidades de prueba sin aumentar nuestro código de prueba?

## `assert` <a id="assert"></a>

-   El comando `assert` de Python nos permite indicar al compilador que algo, alguna afirmación, es verdadera. Podemos aplicar esto a nuestro código de prueba de la siguiente manera:

        from calculator import square


        def main():
            test_square()


        def test_square():
            assert square(2) == 4
            assert square(3) == 9


        if __name__ == "__main__":
            main()

    Observa que estamos afirmando definitivamente cuánto deberían ser `square(2)` y `square(3)`. Nuestro código se reduce de cuatro líneas de prueba a dos.

-   Podemos romper intencionalmente nuestro código de la calculadora modificándolo de la siguiente manera:

        def main():
            x = int(input("¿Cuál es el valor de x? "))
            print("El cuadrado de x es", square(x))


        def square(n):
            return n + n


        if __name__ == "__main__":
            main()

    Observa que hemos cambiado el operador `*` por un `+` en la función square.

-   Ahora, al ejecutar `python test_square.py` en la ventana de la consola, notarás que el compilador genera un `AssertionError`. Básicamente, esto nos indica que una de nuestras condiciones no se cumplió.
-   Uno de los desafíos a los que nos enfrentamos ahora es que nuestro código podría volverse aún más engorroso si quisiéramos proporcionar una salida de error más descriptiva a nuestros usuarios. De manera plausible, podríamos programar de la siguiente manera:

        from calculator import square


        def main():
            test_square()


        def test_square():
            try:
                assert square(2) == 4
            except AssertionError:
                print("El cuadrado de 2 no es 4")
            try:
                assert square(3) == 9
            except AssertionError:
                print("El cuadrado de 3 no es 9")
            try:
                assert square(-2) == 4
            except AssertionError:
                print("El cuadrado de -2 no es 4")
            try:
                assert square(-3) == 9
            except AssertionError:
                print("El cuadrado de -3 no es 9")
            try:
                assert square(0) == 0
            except AssertionError:
                print("El cuadrado de 0 no es 0")


        if __name__ == "__main__":
            main()

    Observa que al ejecutar este código se generarán múltiples errores. Sin embargo, no está produciendo todos los errores mencionados anteriormente. Esto ilustra que es válido probar múltiples casos para detectar posibles errores de codificación.

-   El código anterior ilustra un desafío importante: ¿cómo podríamos facilitar las pruebas de nuestro código sin docenas de líneas de código como las anteriores?

Puedes obtener más información en la documentación de Python sobre [`assert`](https://docs.python.org/3/reference/simple_stmts.html#assert).

## `pytest` <a id="pytest"></a>

-   `pytest` es una biblioteca de terceros que te permite realizar pruebas unitarias en tu programa. Es decir, puedes probar las funciones dentro de tu programa.
-   Para utilizar `pytest`, escribe `pip install pytest` en la ventana de la consola.
-   Antes de aplicar `pytest` a nuestro propio programa, modifica tu función `test_calculator` de la siguiente manera:

        from calculator import square


        def test_assert():
            assert square(2) == 4
            assert square(3) == 9
            assert square(-2) == 4
            assert square(-3) == 9
            assert square(0) == 0

    Observa cómo el código anterior afirma todas las condiciones que queremos probar.

-   `pytest` nos permite ejecutar nuestro programa directamente a través de él, de manera que podamos ver más fácilmente los resultados de nuestras condiciones de prueba.
-   En la ventana del terminal, escribe `pytest test_calculator.py`. Inmediatamente notarás que se proporcionará una salida. Observa la `F` roja cerca de la parte superior de la salida, lo que indica que algo en tu código ha fallado. Además, observa que la `E` roja proporciona algunas pistas sobre los errores en tu programa `calculator.py`. Basándote en la salida, puedes imaginar un escenario en el que `3 * 3` ha producido `6` en lugar de `9`. Según los resultados de esta prueba, podemos corregir nuestro código `calculator.py` de la siguiente manera:

        def main():
            x = int(input("¿Cuál es x? "))
            print("x al cuadrado es", square(x))


        def square(n):
            return n * n


        if __name__ == "__main__":
            main()

    Observa que hemos cambiado el operador `+` a un `*` en la función de cuadrado, devolviéndolo a un estado funcional.

-   Al volver a ejecutar `pytest test_calculator.py`, observa cómo no se producen errores. ¡Felicidades!
-   En este momento, no es ideal que `pytest` se detenga después de la primera prueba fallida. De nuevo, volvamos a dejar nuestro código `calculator.py` en su estado roto:

        def main():
            x = int(input("¿Cuál es x? "))
            print("x al cuadrado es", square(x))


        def square(n):
            return n + n


        if __name__ == "__main__":
            main()

    Observa que hemos cambiado el operador `*` a un `+` en la función de cuadrado, devolviéndolo a un estado roto.

-   Para mejorar nuestro código de prueba, modifiquemos `test_calculator.py` para dividir el código en diferentes grupos de pruebas:

        from calculator import square


        def test_positive():
            assert square(2) == 4
            assert square(3) == 9


        def test_negative():
            assert square(-2) == 4
            assert square(-3) == 9


        def test_zero():
            assert square(0) == 0

    Observa que hemos dividido las mismas cinco pruebas en tres funciones diferentes. Los marcos de pruebas como `pytest` ejecutarán cada función, incluso si hubo un fallo en una de ellas. Al volver a ejecutar `pytest test_calculator.py`, notarás que se muestran muchos más errores. Una mayor salida de errores te permite explorar más a fondo qué podría estar generando los problemas dentro de tu código.

-   Habiendo mejorado nuestro código de prueba, devolvamos el código `calculator.py` a su estado completamente funcional:

        def main():
            x = int(input("¿Cuál es x? "))
            print("x al cuadrado es", square(x))


        def square(n):
            return n * n


        if __name__ == "__main__":
            main()

    Observa que hemos cambiado el operador `+` a un `*` en la función de cuadrado, devolviéndolo a un estado funcional.

-   Al volver a ejecutar `pytest test_calculator.py`, notarás que no se encuentran errores.
-   En resumen, ¡depende de ti como programador definir tantas condiciones de prueba como consideres adecuadas!

Puedes aprender más en la documentación de Pytest de [`pytest`](https://docs.pytest.org/en/7.1.x/getting-started.html).

## Probando Cadenas <a id="testing-strings"></a>

-   Retrocediendo en el tiempo, considera el siguiente código `hello.py`:

        def main():
            name = input("¿Cuál es tu nombre? ")
            hello(name)


        def hello(to="mundo"):
            print("Hola,", to)


        if __name__ == "__main__":
            main()

    Observa que quizás deseemos probar el resultado de la función `hello`.

-   Considera el siguiente código para `test_hello.py`:

        from hello import hello


        def test_hello():
            assert hello("David") == "Hola, David"
            assert hello() == "Hola, mundo"

    Al analizar este código, ¿crees que este enfoque de prueba funcionará bien? ¿Por qué esta prueba podría no funcionar bien? ¡Observa que la función `hello` en `hello.py` imprime algo: es decir, no devuelve un valor!

-   Podemos modificar nuestra función `hello` dentro de `hello.py` de la siguiente manera:

        def main():
            name = input("¿Cuál es tu nombre? ")
            print(hello(name))


        def hello(to="mundo"):
            return f"Hola, {to}"


        if __name__ == "__main__":
            main()

    Observa que cambiamos nuestra función `hello` para que devuelva una cadena. Esto significa efectivamente que ahora podemos usar `pytest` para probar la función `hello`.

-   ¡Al ejecutar `pytest test_hello.py`, nuestro código pasará todas las pruebas!
-   Al igual que con nuestro caso de prueba anterior en esta lección, podemos separar nuestras pruebas por separado:

        from hello import hello


        def test_default():
            assert hello() == "Hola, mundo"


        def test_argument():
            assert hello("David") == "Hola, David"

    Observa que el código anterior separa nuestras pruebas en múltiples funciones de modo que todas se ejecutarán, incluso si se produce un error.

## Organizando Pruebas en Carpetas <a id="organizing-tests-into-folders"></a>

-   Es tan común probar código utilizando múltiples pruebas que tienes la capacidad de ejecutar toda una carpeta de pruebas con un solo comando.
-   Primero, en la ventana de la terminal, ejecuta `mkdir test` para crear una carpeta llamada `test`.
-   Luego, para crear una prueba dentro de esa carpeta, escribe en la ventana de la terminal `code test/test_hello.py`. Observa que `test/` indica a la terminal que cree `test_hello.py` en la carpeta llamada `test`.
-   En la ventana del editor de texto, modifica el archivo para incluir el siguiente código:

        from hello import hello


        def test_default():
            assert hello() == "hello, world"


        def test_argument():
            assert hello("David") == "hello, David"

    Observa que estamos creando una prueba como lo hicimos antes.

-   `pytest` no nos permitirá ejecutar pruebas como una carpeta solo con este archivo (o todo un conjunto de archivos) sin un archivo especial `__init__`. En tu ventana de terminal, crea este archivo escribiendo `code test/__init__.py`. Nota el `test/` como antes, así como los guiones bajos a ambos lados de `init`. Incluso dejando vacío este archivo `__init__.py`, `pytest` es informado de que toda la carpeta que contiene `__init__.py` tiene pruebas que pueden ser ejecutadas.
-   Ahora, al escribir `pytest test` en la terminal, puedes ejecutar toda la carpeta `test` de código.

Puedes aprender más en la documentación de Pytest sobre los [mecanismos de importación](https://docs.pytest.org/en/7.1.x/explanation/pythonpath.html?highlight=folder#pytest-import-mechanisms-and-sys-path-pythonpath).

## Resumen <a id="summing-up"></a>

Probar tu código es una parte natural del proceso de programación. Las pruebas unitarias te permiten probar aspectos específicos de tu código. Puedes crear tus propios programas que prueben tu código. Alternativamente, puedes utilizar frameworks como `pytest` para ejecutar tus pruebas unitarias por ti. En esta conferencia, aprendiste sobre...

-   Pruebas unitarias
-   `assert`
-   `pytest`
