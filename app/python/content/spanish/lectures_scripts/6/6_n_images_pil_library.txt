Muy bien, permítanme proponer que dejemos atrás los archivos CSV, pero cabe destacar que no son el único formato de archivo que se puede usar para leer o escribir datos. De hecho, son un formato popular, al igual que los archivos de texto sin formato, .txt. Pero puedes almacenar datos de la manera que quieras. Solo elegimos los archivos CSV porque representan cómo se puede leer y escribir desde un archivo y hacerlo de manera estructurada, donde de alguna manera se pueden tener múltiples claves, múltiples valores, todos en el mismo archivo sin tener que recurrir a lo que se conoce como un archivo binario.

Entonces, un archivo binario es un archivo que consiste solo en ceros y unos. Y se pueden organizar de cualquier manera que desees, especialmente si quieres almacenar información no textual, sino gráfica, de audio o video también. Resulta que Python es realmente bueno en cuanto a tener bibliotecas para absolutamente todo.

De hecho, hay una biblioteca popular llamada pillow que te permite navegar archivos de imagen y realizar operaciones en ellos. Puedes aplicar filtros, al estilo de Instagram. También puedes animarlos. Así que lo que propongo es dejar los archivos de texto por ahora y abordar una demostración más, esta vez, enfocándonos en esta biblioteca en particular y en archivos de imagen.

Así que permítanme proponer que vayamos a VS Code y creemos un programa que finalmente cree un GIF animado. Estas cosas están en todas partes en forma de memes, animaciones, pegatinas y demás. Y un GIF animado es simplemente un archivo de imagen que contiene múltiples imágenes en su interior.

Y tu computadora o teléfono te muestra esas imágenes una tras otra, a veces en un bucle infinito, una y otra vez. Y siempre que haya suficientes imágenes, crea la ilusión de animación porque tu mente y la mía rellenan visualmente los espacios vacíos y asumen que si algo se está moviendo, aunque solo veas un cuadro por segundo, o una secuencia de eso, parece una animación. Es como una versión simplificada de un archivo de video.

Bueno, permítanme proponer que comencemos con tal vez un par de disfraces de otro lenguaje de programación popular. Y permítanme abrir mi primer disfraz aquí, número 1. Supongamos que este es un disfraz o, en realidad, solo una imagen estática aquí, costume1.gif. Y es solo una imagen estática de un gato, sin movimiento en absoluto.

Permítanme ahora abrir otro, costume2.gif, que se ve un poco diferente. Observen, y volveré atrás y adelante, las patas de este gato están un poco alineadas de manera diferente, por lo que esta era la versión 1, y esta era la versión 2. Ahora, estos gatos provienen de un lenguaje de programación del MIT llamado Scratch que te permite, de manera muy gráfica, animar todo esto y más. Pero usaremos solo estas dos imágenes estáticas, costume1 y costume2, para crear nuestro propio GIF animado que luego podrían enviarle a un amigo o enviárselo como un meme en línea.

Bueno, permítanme proponer que creemos este GIF animado, no utilizando un programa que descargamos, sino escribiendo nuestro propio código. Permítanme ejecutar el código de costumes.py y crear nuestro propio programa que tomará, como entrada, dos o incluso más archivos de imagen y generará un GIF animado a partir de ellos, creando este GIF animado alternando infinitamente entre esas dos imágenes.

Bueno, ¿cómo voy a hacer esto? Bueno, supongamos que este será un programa llamado costumes.py que espera dos argumentos en la línea de comandos, los nombres de los archivos, los disfraces individuales que queremos animar adelante y atrás. Para hacer eso, voy a importar sys para que finalmente tengamos acceso a sys.argv. Luego, desde esta biblioteca pillow, voy a importar el soporte para imágenes específicamente. Entonces, desde PIL importo Image, con I mayúscula, según la documentación de la biblioteca.

Ahora me voy a crear una lista vacía llamada images, solo para tener una lista en la que almacenar una, dos o más de estas imágenes. Y ahora voy a hacer esto. Por cada argumento en sys.argv, voy a crear una nueva variable de imagen, establecerla igual a esta función Image.open, pasando arg. ¿Qué estoy haciendo aquí?

Propongo que eventualmente quiero poder ejecutar python de costumes.py y luego, como argumento en la línea de comandos, especificar costume1.gif, espacio, costume2.gif. Así que quiero tomar esos nombres de archivo desde la línea de comandos como mis argumentos. ¿Entonces, qué estoy haciendo aquí? Bueno, estoy iterando sobre sys.argv todas las palabras en mis argumentos de línea de comandos. Estoy creando una variable llamada imagen y estoy pasando a esta función, Image.open de la biblioteca pillow, ese argumento específico. Y esa biblioteca básicamente va a abrir esa imagen de una manera que me brinda mucha funcionalidad para manipularla, como animarla.

Ahora voy a agregar a mi lista de imágenes esa imagen en particular. Y eso es todo. El propósito de este bucle en la vida es simplemente iterar sobre los argumentos de línea de comandos y abrir esas imágenes usando esta biblioteca. La última línea es bastante sencilla. Voy a hacer esto.

Voy a tomar la primera de esas imágenes, que estará en mi lista en la ubicación 0, y voy a guardarla en el disco. Es decir, voy a guardar este archivo. Ahora, en el pasado, cuando usamos archivos CSV o archivos de texto, tenía que abrir el archivo, escribir en él, tal vez incluso cerrarlo. No necesito hacer eso con esta biblioteca. La biblioteca pillow se encarga de abrir, cerrar y guardar por mí simplemente llamando a save.

Voy a llamar a esta función save. Y solo para dejar espacio, porque tengo varios argumentos para pasar, voy a pasar a otra línea para que quepa. Voy a pasar el nombre del archivo que quiero crear, costumes.gif. Ese será el nombre de mi GIF animado. Le diré a esta biblioteca que guarde todos los cuadros que le paso, por lo que el primer disfraz, el segundo disfraz y aún más si los tuviera.

Luego, voy a agregar a esta primera imagen, las imágenes 0, las siguientes imágenes, es decir, esta lista de imágenes. Y esto es un poco ingenioso, pero voy a hacer esto. Quiero agregar la siguiente imagen allí, images[1]. Y ahora quiero especificar una duración de 200 milisegundos para cada uno de estos cuadros, y quiero que esto se repita para siempre. Y si especificas loop=0, es decir, tiempo 0, significa que no va a repetirse un número finito de veces, sino un número infinito de veces en su lugar.

Y necesito hacer otra cosa. Recuerda que sys.argv no solo contiene las palabras que escribí después del nombre del programa, ¿pero qué más contiene sys.argv? Si piensas en nuestra discusión de los argumentos de línea de comandos, ¿qué más hay en sys.argv además de las palabras que estoy a punto de escribir, como costume1.gif y costume2?

AUDIENCIA: Sí, realmente obtenemos el nombre original del programa que queremos ejecutar, costumes.py.

DAVID MALAN: En efecto, obtenemos el nombre original del programa, costumes.py en este caso, que obviamente no es un GIF. Así que recuerda que usando rebanadas (slices) en Python puedo hacer esto. Si sys.argv es una lista y queremos obtener una rebanada de esa lista, todo después del primer elemento, podemos hacer 1: , que indica que comience en la posición 1, no en 0, y tome una rebanada hasta el final. Así que dame todo excepto la primera cosa en esa lista, que, según lo que dijo McKenzie, es el nombre del programa.

Ahora, si no he cometido ningún error, veamos qué sucede. Voy a ejecutar python de costumes.py, y ahora voy a especificar las dos imágenes que quiero animar, así que costume1.gif y costume2.gif. ¿Qué hará el código ahora? Bueno, para recapitular, estamos usando la biblioteca sys para acceder a esos argumentos de línea de comandos. Estamos usando la biblioteca pillow para tratar esos archivos como imágenes y con toda la funcionalidad que viene con esa biblioteca. Estoy usando esta lista de imágenes solo para acumular todas estas imágenes, una a la vez desde la línea de comandos.

Y en las líneas 7 a 9, solo uso un bucle para iterar sobre todas ellas y agregarlas a esta lista después de abrirlas con la biblioteca. Y el último paso, que realmente es solo una línea de código dividida en tres para que todo encaje, voy a guardar la primera imagen, pero le estoy pidiendo a la biblioteca que también le agregue esta otra imagen, no [0], sino [1]. Y si tuviera más, también podría expresarlas.

Quiero guardar todos estos archivos juntos. Quiero hacer una pausa de 200 milisegundos, un quinto de segundo entre cada cuadro. Y quiero que se repita un número infinito de veces.

Entonces, ahora, si cruzo los dedos como siempre, presiono Enter, no ha sucedido nada malo y eso casi siempre es algo bueno. Ahora voy a ejecutar code de costumes.gif para abrir en VS Code la imagen final. ¿Y lo que creo que debería ver es un gato muy feliz? Y efectivamente.